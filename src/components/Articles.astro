---
import { articles } from '../data/articles';
---

<section id="articles">
  <div class="mx-auto max-w-6xl px-6 py-20">
    <div class="animate-on-scroll stagger-1 mb-12">
      <p class="text-sm font-semibold uppercase tracking-[0.35em] text-accent">Insights</p>
      <h2 class="mt-3 text-4xl text-text-primary md:text-5xl">Technical Writing</h2>
      <div class="mt-4 h-1 w-24 rounded-full bg-gradient-to-r from-accent to-highlight"></div>
    </div>

    <div class="flex snap-x gap-6 overflow-x-auto pb-4 md:grid md:grid-cols-3 md:overflow-visible md:pb-0">
      {
        articles.map((article, index) => (
          <article
            class={`animate-on-scroll stagger-${(index % 6) + 1} min-w-[18rem] shrink-0 snap-start rounded-2xl border border-border bg-bg-card p-6 shadow-lg shadow-black/10 transition duration-300 hover:-translate-y-1.5 hover:border-accent/50 md:min-w-0`}
          >
            <div class="text-4xl">{article.icon}</div>
            <h3 class="mt-5 text-2xl font-semibold text-text-primary">{article.title}</h3>
            <p class="mt-4 text-sm leading-7 text-text-secondary">{article.description}</p>
            <a
              href={article.url}
              class="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-accent transition hover:text-highlight"
              target="_blank"
              rel="noreferrer"
            >
              Read on Medium
              <span aria-hidden="true">→</span>
            </a>
          </article>
        ))
      }
    </div>
  </div>
</section>
